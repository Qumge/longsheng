<%= render 'agents/header' %>
<div class="row">
  <!-- left column -->
  <div class="col-md-10">
    <!-- general form elements -->
    <div class="box box-primary col-md-6">
      <div class="box-header with-border">
        <h3 class="box-title"><%= get_title params %></h3>
      </div>
      <!-- /.box-header -->
      <!-- form start -->
      <%= render partial: 'agents/form', locals: {agent: @agent} %>
    </div>
  </div>
</div>