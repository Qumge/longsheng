<li class="time-label">
                  <span class="bg-purple">
                    进度款
                  </span>
</li>

<li>
  <i class="fa fa-file-pdf-o bg-green"></i>

  <div class="timeline-item">
    <div class="form-group">

    </div>
    <div class="timeline-body">
      <label for="exampleInputFile">进度款资料</label>
      <div >
        <a class="btn btn-default " id="payments" href="#" >
          <i class="fa fa-plus"></i>
          <span>选择文件</span>
        </a>
      </div>

      <% if @project.payments.present? %>
        <% @project.payments.each do |payment| %>
          <div>
          <a href="javascript:void(0)" onclick="show_file(this)" class="btn show-file" data-url="<%= payment.preview_url%>" data-file-name="<%= payment.file_name %>">
            <i class="fa fa-eye"></i><%= payment.file_name %>
          </a>
          <%= link_to 'JavaScript:void(0)', onclick: "delete_attachment(#{payment.id}, '#{delete_attachment_project_path @project.id}')",class: 'btn  btn-sm ', style: 'color: red' do %>
            删除
          <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</li>